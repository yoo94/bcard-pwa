import "./Footer.css"
import Button from "./Button"
const Footer = () => {
  return (
    <footer className="Footer">
      <Button text="추가하기+" onclick={() => { }} location="footer" />
    </footer>
  )
}

export default Footer